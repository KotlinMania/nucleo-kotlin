# Rust callers — `nucleo-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `benches` | `benches` | 0 | 0 | 0 | 0 |
| `fzf_oxide-fuzz` | `fzf_oxide_fuzz` | 0 | 0 | 0 | 0 |
| `nucleo` | `nucleo` | 10 | 1 | 1 | 10 |
| `nucleo-matcher` | `nucleo_matcher` | 0 | 0 | 0 | 0 |

## `benches`

_No cross-repo callers in the workspace._

## `fzf_oxide-fuzz`

_No cross-repo callers in the workspace._

## `nucleo`

### Inbound symbols (10 distinct, 10 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `nucleo`.

#### `nucleo::Config` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:9` — `use nucleo::Config`

#### `nucleo::Injector` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:10` — `use nucleo::Injector`

#### `nucleo::Matcher` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:11` — `use nucleo::Matcher`

#### `nucleo::Nucleo` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:12` — `use nucleo::Nucleo`

#### `nucleo::Utf32Str` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:31` — `use nucleo::Utf32Str`

#### `nucleo::Utf32String` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:13` — `use nucleo::Utf32String`

#### `nucleo::pattern::AtomKind` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:33` — `use nucleo::pattern::AtomKind`

#### `nucleo::pattern::CaseMatching` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:14` — `use nucleo::pattern::CaseMatching`

#### `nucleo::pattern::Normalization` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:15` — `use nucleo::pattern::Normalization`

#### `nucleo::pattern::Pattern` — 1 site(s) (1 codex, 0 other, 1 caller crates)

- ✅ `codex-kotlin/codex-file-search` `src/lib.rs:35` — `use nucleo::pattern::Pattern`

## `nucleo-matcher`

_No cross-repo callers in the workspace._
