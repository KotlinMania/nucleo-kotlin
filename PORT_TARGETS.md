# Port targets — `nucleo-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `nucleo` that codex-rs uses, and therefore the minimum subset that `nucleo-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-83] (rank `185`, tier `direct`, status `To Do`)

**Cargo package name**: `nucleo`
**Rust module name (in `use` statements)**: `nucleo`

## Summary

- **10** distinct symbols imported by codex-rs
- **10** total `use` statements
- **1** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `nucleo::Config`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:9` (`codex-file-search`) — `use nucleo::Config`

### `nucleo::Injector`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:10` (`codex-file-search`) — `use nucleo::Injector`

### `nucleo::Matcher`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:11` (`codex-file-search`) — `use nucleo::Matcher`

### `nucleo::Nucleo`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:12` (`codex-file-search`) — `use nucleo::Nucleo`

### `nucleo::Utf32String`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:13` (`codex-file-search`) — `use nucleo::Utf32String`

### `nucleo::pattern::CaseMatching`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:14` (`codex-file-search`) — `use nucleo::pattern::CaseMatching`

### `nucleo::pattern::Normalization`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:15` (`codex-file-search`) — `use nucleo::pattern::Normalization`

### `nucleo::Utf32Str`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:31` (`codex-file-search`) — `use nucleo::Utf32Str`

### `nucleo::pattern::AtomKind`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:33` (`codex-file-search`) — `use nucleo::pattern::AtomKind`

### `nucleo::pattern::Pattern`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-file-search`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `file-search/src/lib.rs:35` (`codex-file-search`) — `use nucleo::pattern::Pattern`
