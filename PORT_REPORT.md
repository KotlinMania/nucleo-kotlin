# Port report — `nucleo-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **4** member crate(s): `benches`, `fzf_oxide-fuzz`, `nucleo`, `nucleo-matcher`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 10 `use` sites from 1 other repos
- **Outbound**: 1 `use` sites to 1 other repos
- **Unresolved external**: 7 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `benches` | `benches` | 0 | 0 | — | — | — |
| `fzf_oxide-fuzz` | `fzf_oxide_fuzz` | 0 | 0 | — | — | — |
| `nucleo` | `nucleo` | 10 | 0 | COD-83 | 185 | To Do |
| `nucleo-matcher` | `nucleo_matcher` | 0 | 1 | — | — | — |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (10 edges, 1 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `codex-kotlin` | `codex-file-search` | 10 | ✅ |

### Symbol-level demand (10 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `nucleo::Config` | 1 | 1 | 0 | 1 |
| `nucleo::Injector` | 1 | 1 | 0 | 1 |
| `nucleo::Matcher` | 1 | 1 | 0 | 1 |
| `nucleo::Nucleo` | 1 | 1 | 0 | 1 |
| `nucleo::Utf32Str` | 1 | 1 | 0 | 1 |
| `nucleo::Utf32String` | 1 | 1 | 0 | 1 |
| `nucleo::pattern::AtomKind` | 1 | 1 | 0 | 1 |
| `nucleo::pattern::CaseMatching` | 1 | 1 | 0 | 1 |
| `nucleo::pattern::Normalization` | 1 | 1 | 0 | 1 |
| `nucleo::pattern::Pattern` | 1 | 1 | 0 | 1 |

#### Per-symbol detail (top 10 symbols)

##### `nucleo::Config` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:9` — `use nucleo::Config`

##### `nucleo::Injector` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:10` — `use nucleo::Injector`

##### `nucleo::Matcher` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:11` — `use nucleo::Matcher`

##### `nucleo::Nucleo` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:12` — `use nucleo::Nucleo`

##### `nucleo::Utf32Str` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:31` — `use nucleo::Utf32Str`

##### `nucleo::Utf32String` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:13` — `use nucleo::Utf32String`

##### `nucleo::pattern::AtomKind` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:33` — `use nucleo::pattern::AtomKind`

##### `nucleo::pattern::CaseMatching` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:14` — `use nucleo::pattern::CaseMatching`

##### `nucleo::pattern::Normalization` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:15` — `use nucleo::pattern::Normalization`

##### `nucleo::pattern::Pattern` (1 sites, 1 codex, 0 other, 1 caller crates)

Codex call sites:

- `codex-kotlin/codex-file-search` `src/lib.rs:35` — `use nucleo::pattern::Pattern`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `unicode-segmentation-kotlin` | 1 | 1 | `unicode-segmentation` | COD-177 | 116 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

| package | edges | distinct symbols | importing members in this repo |
|---|---:|---:|---|
| `parking_lot` | 88 | 19 | `nucleo` |
| `rayon` | 30 | 15 | `nucleo` |
| `memchr` | 18 | 13 | `nucleo-matcher` |
| `fzf_oxide` | 4 | 4 | `fzf_oxide-fuzz` |
| `libfuzzer-sys` | 3 | 2 | `fzf_oxide-fuzz` |
| `brunch` | 2 | 2 | `benches` |
| `fuzzy-matcher` | 1 | 1 | `benches` |
